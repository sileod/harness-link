#!/usr/bin/env python3
import os
from pathlib import Path
import sys

runtime = Path(__file__).resolve().with_name("harness-link")
os.execv(sys.executable, [sys.executable, str(runtime), "inferx", *sys.argv[1:]])
